machine:
  services:
    - docker

dependencies:
  override:
#    - docker pull vvakame/review
    - gem install review

test:
  override:
#    - docker run -t -i vvakame/review /bin/bash
    - git clone https://github.com/vvakame/AndroidOpenTextbook.git
    - cd AndroidOpenTextbook
    - npm install
    - grunt clean pdf
